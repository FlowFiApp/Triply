import { CarDetails } from "@/components/screens/CarRental";
import ThemeSync from "@/components/ThemeSync";

export default function Page() {
  return (
    <>
      <ThemeSync />
      <CarDetails />
    </>
  );
}