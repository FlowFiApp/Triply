import { CarSearch } from "@/components/screens/CarRental";
import ThemeSync from "@/components/ThemeSync";

export default function Page() {
  return (
    <>
      <ThemeSync />
      <CarSearch />
    </>
  );
}